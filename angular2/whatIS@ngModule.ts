***they allow to group the functionality of our app into logical parts, 
and are essential for enabling ahead of time compilation (which means much better bootstrap performance) and lazy-loading.

Angular module is used to group an inter-related set of Angular primitives like(components, directives, pipes, services).


build everything into separate modules to separate the functionality of your application. 